rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // SIMPLIFIED RULES FOR TESTING - Allows all authenticated users to read/write
    // Replace with the more secure rules in firestore.rules for production
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
